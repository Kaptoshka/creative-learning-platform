---
extends: default

ignore: |
    **/node_modules/

rules:
    line-length:
        max: 120
        level: warning
    comments:
        min-spaces-from-content: 1
    braces:
        max-spaces-inside: 1
    truthy:
        allowed-values: ["true", "false"]
        check-keys: false
    indentation:
        spaces: 4
        indent-sequences: true
